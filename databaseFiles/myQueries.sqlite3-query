-- database: ./database.db
-- INSERT INTO id7-tusers(username,password) VALUES ("","");
-- SELECT * FROM extension;

-- INSERT INTO Quizzer(question_number ,paper_year ,question,a ,b ,c ,d ,correct_answer ,core , topic_question ,dot_point,point)
-- VALUES ("","","","","","","","","","","","");


--CREATE TABLE Quizzer(id INTEGER PRIMARY KEY autoincrement,question_number int,paper_year int,question TEXT NOT NULL,a TEXT,b TEXT,c TEXT,d TEXT,correct_answer TEXT NOT NULL,core TEXT NOT NULL, topic_question TEXT NOT NULL,dot_point TEXT NOT NULL,point TEXT NOT NULL);

-- INSERT INTO Questions(question_number ,paper_year ,question,a ,b ,c ,d)
-- VALUES ("16","2022",
-- "Why would an athlete in a weight lifting competition consider using a caffeine supplement
-- 30 minutes prior to the event?",
-- "The availability of fat stores will be stimulated so that they can be used as a source
-- of fuel.",
-- "The athlete will retain fluid levels, improve their glycogen stores and feel less
-- fatigued.",
-- "The central nervous system will be stimulated, increasing trait anxiety and
-- improving concentration.",
-- "The central nervous system will be stimulated, increasing the release of adrenaline
-- and enhancing muscular contractions.");

UPDATE Questions
SET question = 'The player profile for Athlete A is shown.
Athlete A: Athletic build, Height: 195 cm (6 ft 4 in), Hardworking, Willingness to listen
What do the characteristics of Athlete A relate to?'
WHERE question_id = 4;

-- UPDATE Questions
-- SET core = '1',
--     topic = 'identifying priority health issues'
-- WHERE question_id = 36;

-- measuring health status
-- identifying priority health issues
-- groups experiencing health inequities
-- high levels of preventable chronic disease, injury and mental health problems
-- a growing and ageing population
-- health care in Australia
-- complementary and alternative health care approaches
-- health promotion based on the five action areas of the Ottawa Charter

-- energy systems
-- types of training and training methods
-- principles of training
-- physiological adaptations in response to training
-- motivation
-- anxiety and arousal
-- psychological strategies to enhance motivation and manage anxiety
-- nutriional considerations
-- supplementation
-- recovery strategies
-- stages of skill acquisition
-- characteristics of the learner
-- the learning environment
-- assessment of skill and performance










-- ALTER TABLE Questions
-- ADD COLUMN topic TEXT;

-- ALTER TABLE Quizzer
-- RENAME to Questions;

-- CREATE TABLE Quizzes(quiz_id INTEGER PRIMARY KEY autoincrement, FOREIGN KEY (question_id) REFERENCES Questions(question_id), mark BOOLEAN, core, topic);

-- ALTER TABLE Questions
-- RENAME COLUMN id to question_id;

-- CREATE TABLE Quizzes(id INTEGER PRIMARY KEY AUTOINCREMENT,
-- quiz_id INTEGER,
-- question_id INTEGER,
-- mark BOOLEAN,
-- core TEXT,
-- topic TEXT,
-- FOREIGN KEY (question_id) REFERENCES Questions(question_id)
-- );

DELETE FROM Quizzes;

-- ALTER TABLE Quizzes ADD COLUMN selected_answer TEXT;
-- ALTER TABLE Quizzes ADD COLUMN correct_answer TEXT;